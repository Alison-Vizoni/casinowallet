package com.casinowallet.casinowallet.service.integration;

import org.junit.jupiter.api.extension.ExtendWith;
import org.springframework.test.context.junit.jupiter.SpringExtension;

import java.util.logging.Logger;

@ExtendWith(SpringExtension.class)
public class DirectIntegrationServiceTest {

    private final Logger LOG = Logger.getLogger(DirectIntegrationServiceTest.class.getName());

}
